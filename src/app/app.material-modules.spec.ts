import { App.MaterialModules } from './app.material-modules';

describe('App.MaterialModules', () => {
  it('should create an instance', () => {
    expect(new App.MaterialModules()).toBeTruthy();
  });
});
